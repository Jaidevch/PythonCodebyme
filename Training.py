#This is the code for identifying the small number than 10
def is_small(x):
    if (x < 10):
        return (True)
    else:
        return (False)
    return ()
print(is_small(1))

#This is the password checker program
def check(x):
    if (x == "secret"):
        return (True)
    else:
        return (False)
    return (x)
check("secret")

